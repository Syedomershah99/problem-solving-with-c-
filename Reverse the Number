#include <iostream>
using namespace std;

int main() {
    int X;
    cin >> X;

    int reversed = 0;

    // Reverse the number
    while (X > 0) {
        int digit = X % 10;
        reversed = reversed * 10 + digit;
        X /= 10;
    }

    cout << reversed << endl;

    return 0;
}
